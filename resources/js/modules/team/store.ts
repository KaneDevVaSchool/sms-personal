import { defineStore } from 'pinia';

/**
 * Team module store — scaffold for future client state (filters, UI preferences, etc.).
 */
export const useTeamModuleStore = defineStore('teamModule', () => {
    return {};
});
